# 任务：双层知识库 — claude-howto

> 创建日期：2026-04-01

---

## 里程碑

| # | 里程碑 | 目标时间 | 描述 |
|---|-----------|--------|-------------|
| M1 | 基础设施上线 | 2026年4月底 | quickstart.sh、CLAUDE.md、过期检查 action、agent 索引生成器上线；2个最弱模块升级 |
| M2 | 6/10 模块完成 | 2026年6月底 | Slash commands、memory、skills、CLI 模块完全升级；生成器产出有效 manifest |
| M3 | 所有10个模块完成 | 2026年8月底 | 每个模块都有场景介绍、Try It Now 块、决策树、命名模式 |
| M4 | Agent 层上线 | 2026年9月底 | agent-manifest.json + AGENT-INDEX.md 生成；仓库内置查询 skill；RECIPES.md + COMMUNITY-PROJECTS.md 上线 |
| M5 | 版本审计完成 | 2026年11月底 | 所有10个模块针对当前 CC 版本验证；RECIPES.md 达到 8+ 配方 |
| M6 | 自主运行系统 | 2027年3月底 | Agent manifest 覆盖100%模块（CI 验证）；RECIPES.md 达到 10+；社区页面有机增长 |

---

## 阶段1 — 基础设施和弱模块（2026年4月）

**目标里程碑：M1**

### 支柱4：新人 Onboarding

- [ ] **T-001** — 编写 `scripts/quickstart.sh`
  - 检测 `~/.claude/` 目录（如果缺失则询问用户确认后创建）
  - 复制第一个 slash command + CLAUDE.md + skill 到用户设置
  - 在 CLAUDE.md 追加 agent-discovery 行，链接到 AGENT-INDEX.md
  - 幂等：跳过已有文件并警告，永不覆盖
  - 完成后打印后续步骤

- [ ] **T-002** — 创建 `QUICKSTART.md`（前15分钟视觉指南）
  - 终端步骤作为代码块注释（尽量避免 PNG 截图；优先使用 ASCII 或 Mermaid）
  - 记录每个可视化步骤对应的命令，以便重新捕获

- [ ] **T-003** — 在所有10个模块的 README 添加难度徽章
  - 格式：每个模块 README 顶部添加 `> 🟢 **Beginner** | ⏱ 30 minutes`
  - 在徽章下方添加 "What you'll build" 预览 bullet

### 支柱7：活课程

- [ ] **T-004** — 创建 `WHATS-NEW.md`
  - 格式：`## CC vX.X — DATE`，每个功能变更一个 bullet 项
  - 至少为当前 CC 版本添加一条记录

- [ ] **T-005** — 在所有10个模块的 README 添加版本徽章
  - 格式：`> ✅ Verified against Claude Code **vX.X.X** · Last verified: YYYY-MM-DD`
  - 在每个模块的 frontmatter 中添加 `cc_version_verified`

- [ ] **T-006** — 创建 `.github/workflows/staleness-check.yml`
  - 计划：每周一 09:00 UTC
  - 读取每个模块 frontmatter 中的 `last_verified` 日期（使用 `yq` 或10行 Python 脚本）
  - 为30天以上未验证的模块创建 GitHub Issue
  - 如果已存在同标题的 open issue，则跳过

### 支柱6：内容质量（CLAUDE.md）

- [ ] **T-007** — 为项目创建根 `CLAUDE.md`
  - 作为项目自身配置，展示最佳实践
  - 记录贡献约定、模块结构和维护工作流

### 支柱2：AI Agent 索引（生成器）

- [ ] **T-008** — 编写 `scripts/build-agent-index.py`
  - 读取所有10个模块的 README 文件
  - 提取：标题（→ topics）、代码块（→ 带语言标签的 examples）、表格（→ 参考数据）
  - 输出 `agent-manifest.json`，schema 定义于 CEO 计划
  - 输出 `AGENT-INDEX.md`（人类可读的摘要）
  - 验证引用指向实际文件

### 模块 08-checkpoints：完整深度更新（311 → 800+ 行）

- [ ] **T-009** — 用真实场景重写模块介绍
  - 示例："Your refactor just broke 3 things. Here's how checkpoints save you..."

- [ ] **T-010** — 添加 checkpoint 策略决策树（Mermaid 流程图）
  - "I want to do X" → 跟随箭头 → 落在 checkpoint 模式上

- [ ] **T-011** — 添加3个工作流模板
  - 安全重构工作流
  - 使用 checkpoints 的 A/B 测试
  - 灾难恢复工作流

- [ ] **T-012** — 添加与规划模式集成的章节

- [ ] **T-013** — 添加 checkpoint 生命周期的 Mermaid 图

- [ ] **T-014** — 添加2+个"Try It Now"块，包含可验证的命令
  - 每个包含：上下文句子、要运行的命令、预期输出描述

- [ ] **T-015** — 添加"Patterns & Recipes"章节，包含2+个命名模式
  - 每个模式：problem、solution code、何时使用、何时不使用

### 模块 06-hooks：完整深度更新

- [ ] **T-016** — 用真实场景重写模块介绍
  - 示例："Your CI caught a lint error after 20 minutes. Hooks catch it in 2 seconds..."

- [ ] **T-017** — 添加 hooks 决策树（Mermaid 流程图）
  - "I want to trigger X when Y happens" → 跟随箭头 → 落在 hook 类型上

- [ ] **T-018** — 添加5个完整 hook 示例，包含完整脚本（约80行每个）
  1. 写入时自动格式化（prettier/black）
  2. 提交时安全扫描（trufflehog/bandit）
  3. 文件更改时运行测试
  4. 会话结束时通知
  5. Prompt 验证（阻止危险模式）

- [ ] **T-019** — 添加2+个"Try It Now"块，包含可验证的命令

- [ ] **T-020** — 添加"Patterns & Recipes"章节，包含2+个命名模式

**M1 检查点：基础设施上线，2个最弱模块完全升级**

---

## 阶段2 — 深度更新：强模块批次1（2026年5–6月）

**目标里程碑：M2**

### 模块 01-slash-commands：完整深度更新

- [ ] **T-021** — 用场景重写介绍（"Your teammate just pushed 47 files..."）
- [ ] **T-022** — 添加决策树（Mermaid）
- [ ] **T-023** — 添加2+个"Try It Now"块
- [ ] **T-024** — 添加"Patterns & Recipes"章节，包含2+个命名模式

### 模块 02-memory：完整深度更新

- [ ] **T-025** — 用真实场景重写介绍
- [ ] **T-026** — 添加决策树（Mermaid）
- [ ] **T-027** — 添加2+个"Try It Now"块
- [ ] **T-028** — 添加"Patterns & Recipes"章节，包含2+个命名模式

### 模块 03-skills：完整深度更新

- [ ] **T-029** — 用真实场景重写介绍
- [ ] **T-030** — 添加决策树（Mermaid）
- [ ] **T-031** — 添加2+个"Try It Now"块
- [ ] **T-032** — 添加"Patterns & Recipes"章节，包含2+个命名模式

### 模块 10-cli：完整深度更新

- [ ] **T-033** — 用真实场景重写介绍
- [ ] **T-034** — 添加决策树（Mermaid）
- [ ] **T-035** — 添加2+个"Try It Now"块
- [ ] **T-036** — 添加"Patterns & Recipes"章节，包含2+个命名模式

### 生成器验证

- [ ] **T-037** — 每次模块升级后运行 `scripts/build-agent-index.py`；确认有效的 manifest 输出
- [ ] **T-038** — 添加 CI 步骤：每次 push 时验证 agent-manifest.json schema

**M2 检查点：6/10 模块完成，生成器产出有效 manifest**

---

## 阶段3 — 深度更新：强模块批次2（2026年7–8月）

**目标里程碑：M3**

### 模块 04-subagents：完整深度更新

- [ ] **T-039** — 用真实场景重写介绍
- [ ] **T-040** — 添加决策树（Mermaid）
- [ ] **T-041** — 添加2+个"Try It Now"块
- [ ] **T-042** — 添加"Patterns & Recipes"章节（如 "The Multi-Agent Review Pattern"）

### 模块 05-mcp：完整深度更新

- [ ] **T-043** — 用真实场景重写介绍
- [ ] **T-044** — 添加决策树（Mermaid）
- [ ] **T-045** — 添加2+个"Try It Now"块
- [ ] **T-046** — 添加"Patterns & Recipes"章节，包含2+个命名模式

### 模块 07-plugins：完整深度更新

- [ ] **T-047** — 用真实场景重写介绍
- [ ] **T-048** — 添加决策树（Mermaid）
- [ ] **T-049** — 添加2+个"Try It Now"块
- [ ] **T-050** — 添加"Patterns & Recipes"章节，包含2+个命名模式

### 模块 09-advanced-features：完整深度更新

- [ ] **T-051** — 用真实场景重写介绍
- [ ] **T-052** — 添加决策树（Mermaid）
- [ ] **T-053** — 添加2+个"Try It Now"块
- [ ] **T-054** — 添加"Patterns & Recipes"章节，包含2+个命名模式

**M3 检查点：所有10个模块完成且一致**

---

## 阶段4 — Agent 层和跨模块（2026年9月）

**目标里程碑：M4**

### 支柱2：最终确定 AI Agent 索引

- [ ] **T-055** — 对所有10个已完成的模块运行 `scripts/build-agent-index.py`
  - 生成最终 `agent-manifest.json`，100%模块覆盖
  - 生成 `AGENT-INDEX.md` 并从 README.md 链接

- [ ] **T-056** — 创建 `skills/claude-howto-lookup/SKILL.md`
  - 读取 `agent-manifest.json` 查找相关模块
  - 使用 `Read` 加载章节
  - 安装方式：`cp -r claude-howto/skills/claude-howto-lookup ~/.claude/skills/`

- [ ] **T-057** — 在 quickstart.sh 和 QUICKSTART.md 中提及查询 skill 作为可选功能

### 支柱3b：跨模块 RECIPES.md

- [ ] **T-058** — 创建 `RECIPES.md`，包含5+个复合工作流
  - 每个配方的 schema：name、modules-used、problem、solution code block、expected outcome
  - 示例：skill + hook + subagent = 自动化审查 pipeline
  - 确保所有主要标题都有 HTML 锚点以便深度链接

### 支柱5：社区展示

- [ ] **T-059** — 创建 `COMMUNITY-PROJECTS.md`
  - 静态 Markdown 页面，提交格式：name、description、link、features used
  - 包含模板条目
  - 基于 PR 的提交说明

**M4 检查点：agent 索引上线，recipes 上线，社区页面上线**

---

## 阶段5 — 版本审计和配方扩展（2026年10–11月）

**目标里程碑：M5**

- [ ] **T-060** — 运行完整版本审计：验证所有10个模块针对当前 CC 版本
  - 更新每个模块的 `cc_version_verified` frontmatter
  - 更新版本徽章

- [ ] **T-061** — 基于观察到的社区模式，将 `RECIPES.md` 扩展到 8+ 配方

- [ ] **T-062** — 如果从已完成模块中发现新模式，迭代 agent manifest schema

- [ ] **T-063** — 开启置顶 GitHub Discussion："Share your Claude Code workflows"
  - 收集真实 agent 使用的信号，以指导未来配方扩展

**M5 检查点：所有模块验证为最新，recipes 持续增长**

---

## 阶段6 — 复合与持续（2026年12月 – 2027年3月）

**目标里程碑：M6**

- [ ] **T-064** — 每次 CC 发布后运行 `/docs-sync-claude-code` skill；更新 `WHATS-NEW.md`

- [ ] **T-065** — 确保 agent manifest 覆盖100%模块（CI 回归测试）

- [ ] **T-066** — 将 `RECIPES.md` 增长到 10+ 配方

- [ ] **T-067** — 监控 `COMMUNITY-PROJECTS.md` 的有机增长；策展新条目

- [ ] **T-068** — 评估来自 GitHub Discussions 的 agent 使用信号；如果验证通过，投入推广查询 skill（营销、asm registry）

**M6 检查点：系统自主运行**

---

## 推迟事项（TODOS.md）

以下事项不在本计划范围内。记录在此以避免范围蔓延：

- Skill 市场或可安装 registry
- 自定义网站或仪表板
- 完成度追踪基础设施（cc-progress）
- 社区教程模板及 CI 验证
- 自动生成的 CONTRIBUTORS.md
- 多语言翻译
- 问答/评估基础设施升级
- COMMUNITY-PROJECTS.md 的社区投票/审查机制

---

## 按阶段的任务汇总

| 阶段 | 任务 | 时间段 | 涉及的模块 |
|-------|-------|--------|-----------------|
| 1 — 基础设施和弱模块 | T-001 到 T-020 | 2026年4月 | 06-hooks、08-checkpoints + 基础设施 |
| 2 — 批次1深度更新 | T-021 到 T-038 | 2026年5–6月 | 01、02、03、10 |
| 3 — 批次2深度更新 | T-039 到 T-054 | 2026年7–8月 | 04、05、07、09 |
| 4 — Agent 层和跨模块 | T-055 到 T-059 | 2026年9月 | 所有（manifest）+ 新文件 |
| 5 — 版本审计和 Recipes | T-060 到 T-063 | 2026年10–11月 | 所有（审计） |
| 6 — 持续运行 | T-064 到 T-068 | 2026年12月–2027年3月 | 持续进行 |
